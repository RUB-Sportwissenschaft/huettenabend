---
phase: 02-admin-deployment
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified: [valmorel.html, README.md]
autonomous: true
requirements: [DEPL-01, DEPL-02, DEPL-03]

must_haves:
  truths:
    - "valmorel.html ist eine einzige selbststaendige HTML-Datei — kein separates CSS-, JS- oder Bild-File existiert"
    - "README.md enthaelt eine Schritt-fuer-Schritt-Anleitung: JSONBin-Key eintragen, GitHub pushen, Pages aktivieren, WhatsApp-Link teilen"
    - "Die README nennt die exakte Zeile in valmorel.html wo JSONBIN_API_KEY und JSONBIN_BIN_ID einzutragen sind"
    - "Am Ende der README-Anleitung steht die fertige GitHub-Pages-URL im Format https://USERNAME.github.io/REPONAME/"
    - "Ein Entwickler kann in unter 5 Minuten die App deployen wenn er der README folgt"
  artifacts:
    - path: "README.md"
      provides: "Deployment-Anleitung"
      contains: "GitHub Pages"
    - path: "valmorel.html"
      provides: "Vollstaendige Single-File-App (alle Phasen)"
  key_links:
    - from: "README.md Schritt 1"
      to: "valmorel.html Zeile ~336-337"
      via: "Direkte Zeilenangabe in der Anleitung"
      pattern: "JSONBIN_API_KEY|JSONBIN_BIN_ID"
---

<objective>
Single-File-Vollstaendigkeitspruefung und README.md erstellen: Bestaetigen dass valmorel.html alle Ressourcen inline hat, dann eine praezise Deployment-Anleitung schreiben und nach dem GitHub-Username fragen.

Purpose: Organisatoren und Entwickler muessen die App ohne technisches Hintergrundwissen deployen koennen.
Output: README.md mit 5-Minuten-Deployment-Anleitung und die fertige GitHub-Pages-URL.
</objective>

<execution_context>
@C:/Users/kilia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/kilia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/kilia/OneDrive/Desktop/Valmorel/.planning/PROJECT.md
@C:/Users/kilia/OneDrive/Desktop/Valmorel/.planning/ROADMAP.md

<interfaces>
<!-- valmorel.html Struktur nach Plaenen 01-01, 01-02, 02-01, 02-02:                -->
<!-- - Einzige HTML-Datei (alle CSS, JS, Base64-Assets inline)                      -->
<!-- - JSONBIN_API_KEY und JSONBIN_BIN_ID als Konstanten ca. Zeile 336-337           -->
<!-- - Admin-Ansicht erreichbar unter ?admin=true                                    -->
<!-- - PDF-Export via CDN (jsPDF + AutoTable) — Internet-Verbindung fuer Admin noetig-->
<!--                                                                                  -->
<!-- Git-Repo-Name ermitteln:                                                         -->
<!-- git remote get-url origin  -->  zeigt https://github.com/USER/REPO.git          -->
<!-- Falls kein Remote: Nutzer nach Username fragen (DEPL-03)                         -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Single-File-Pruefung und README.md erstellen</name>
  <files>README.md</files>
  <action>
SCHRITT 1 — Single-File-Pruefung:
Pruefen ob valmorel.html wirklich alle Ressourcen inline hat:

  node -e "
    const h = require('fs').readFileSync('valmorel.html', 'utf8');
    const checks = [
      ['Kein externer CSS-Link', !h.match(/&lt;link[^&gt;]+href=.*\.css/)],
      ['Kein externer Script-Src (ausser CDN zur Laufzeit)', !h.match(/&lt;script[^&gt;]+src=.*\.(js)\"/)],
      ['Base64-Logo vorhanden', h.includes('data:image/png;base64')],
      ['JSONBIN_API_KEY-Platzhalter oder echter Key', h.includes('JSONBIN_API_KEY')],
      ['Admin-JS vorhanden', h.includes('initAdmin')],
      ['PDF-JS vorhanden', h.includes('generatePdf')]
    ];
    checks.forEach(([name, ok]) =&gt; console.log((ok?'OK':'FEHLT') + ': ' + name));
  "

Falls eine Pruefung fehlschlaegt: das fehlende Element aus den vorherigen Plaenen nacherfassen, bevor README erstellt wird.

SCHRITT 2 — GitHub-Remote ermitteln:
  git remote get-url origin 2&gt;/dev/null || echo "KEIN_REMOTE"

Falls Ergebnis "KEIN_REMOTE": USERNAME-Platzhalter in README verwenden und Nutzer in der README darauf hinweisen.
Falls Ergebnis vorhanden (z.B. https://github.com/kilia/valmorel.git): USERNAME=kilia, REPONAME=valmorel extrahieren.

SCHRITT 3 — README.md schreiben:
README.md im Projektstamm erstellen. Inhalt auf Deutsch, klar und knapp. Struktur:

---
# Essensumfrage Valmorel

Einfache Essensumfrage fuer Ski-Fahrten. Teilnehmer geben ihre Essenswahl auf dem Handy ab, Organisatoren sehen alle Antworten in einer Tabelle und koennen ein PDF exportieren.

## Schnellstart (unter 5 Minuten)

### 1. JSONBin.io einrichten

1. Konto erstellen: https://jsonbin.io (kostenlos)
2. Neuen Bin anlegen: "+ Create Bin" → Inhalt: `[]` → speichern
3. **Master Key** kopieren (Einstellungen → API Keys)
4. **Bin ID** kopieren (aus der URL: `https://jsonbin.io/app/bins/DIESE_ID`)

### 2. Credentials in valmorel.html eintragen

Datei `valmorel.html` in einem Text-Editor oeffnen. **Zeilen 336–337** aendern:

```
const JSONBIN_API_KEY = 'DEIN_API_KEY_HIER';   // ← deinen Master Key eintragen
const JSONBIN_BIN_ID  = 'DEINE_BIN_ID_HIER';   // ← deine Bin ID eintragen
```

Datei speichern.

### 3. Auf GitHub pushen

```bash
git add valmorel.html
git commit -m "add JSONBin credentials"
git push
```

Noch kein GitHub-Repo? Neues Repo erstellen unter https://github.com/new (Name z. B. `valmorel`), dann:

```bash
git remote add origin https://github.com/USERNAME/valmorel.git
git branch -M main
git push -u origin main
```

### 4. GitHub Pages aktivieren

1. Repo auf GitHub oeffnen → **Settings** → **Pages**
2. Source: **Deploy from a branch** → Branch: `main` → Ordner: `/ (root)` → **Save**
3. Nach ~1 Minute erscheint die URL:

   **https://USERNAME.github.io/valmorel/**

Diese URL per WhatsApp an alle Teilnehmer senden.

### 5. Admin-Auswertung aufrufen

```
https://USERNAME.github.io/valmorel/valmorel.html?admin=true
```

Tabelle zeigt alle Antworten. "PDF herunterladen" exportiert die Auswertung (Internet-Verbindung benoetigt).

---

## Technische Details

| Merkmal | Wert |
|---------|------|
| Deployment | Einzelne HTML-Datei, kein Build-Step |
| Datenspeicherung | JSONBin.io (kostenlos, 10k Requests/Monat) |
| Admin-Zugang | ?admin=true (kein Passwort) |
| PDF-Export | jsPDF + AutoTable (CDN, nur im Admin) |
| Mobile | Mobile-first, ab 320px |

---

*Essensumfrage Valmorel — Erstellt [JAHR]*
---

WICHTIG bei der Datei-Erstellung:
- USERNAME und REPONAME aus git remote befuellen ODER als "USERNAME"/"REPONAME" belassen mit deutlichem Hinweis
- Jahreszahl am Ende: aktuelles Jahr eintragen
- Keine Markdown-Uebersetzung von Umlauten noetig (README ist keine JS-Datei)
- Die README muss mit dem Write-Tool erstellt werden — kein Bash-Heredoc
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const r=fs.readFileSync('README.md','utf8'); ['JSONBin','GitHub Pages','valmorel.html','admin=true','Zeilen 336','PDF'].forEach(k=&gt;console.log(r.includes(k)?'OK: '+k:'FEHLT: '+k)); const h=fs.readFileSync('valmorel.html','utf8'); ['initAdmin','generatePdf','data:image/png;base64'].forEach(k=&gt;console.log(h.includes(k)?'OK html: '+k:'FEHLT html: '+k));"</automated>
  </verify>
  <done>README.md existiert mit allen 6 Schluesselwoertern. valmorel.html besteht Single-File-Pruefung (3 html-Checks OK).</done>
</task>

<task type="auto">
  <name>Task 2: GitHub-Username ermitteln und fertige Pages-URL ausgeben (DEPL-03)</name>
  <files>README.md</files>
  <action>
DEPL-03 verlangt: "Am Ende der HTML-Generierung nach GitHub-Username fragen fuer die fertige GitHub-Pages-URL."

Da das ein Skript-Schritt ist (kein interaktiver Prompt moeglich), folgenden Ablauf ausfuehren:

SCHRITT 1 — Remote ermitteln:
  git remote get-url origin 2>/dev/null

SCHRITT 2a — Falls Remote vorhanden (z.B. https://github.com/kilia/valmorel.git):
  - USERNAME und REPONAME aus der URL extrahieren
  - README.md aktualisieren: alle Vorkommen von "USERNAME" durch echten Usernamen ersetzen, "valmorel" als Reponame bestaetigen
  - Im Terminal ausgeben:

    GitHub-Pages-URL: https://USERNAME.github.io/REPONAME/
    Admin-URL:        https://USERNAME.github.io/REPONAME/valmorel.html?admin=true

    Diese URLs sind bereit zum Teilen sobald GitHub Pages aktiviert ist (Settings -> Pages -> main branch).

SCHRITT 2b — Falls kein Remote vorhanden:
  - README.md bleibt mit "USERNAME"-Platzhalter
  - Im Terminal ausgeben:

    Kein GitHub-Remote gefunden. Bitte USERNAME in README.md manuell ersetzen.
    Beispiel-URL: https://DEIN-USERNAME.github.io/valmorel/
    Admin-URL:    https://DEIN-USERNAME.github.io/valmorel/valmorel.html?admin=true

SCHRITT 3 — Finale Zusammenfassung ins Terminal:
  Ausgeben:
    ===== VALMOREL DEPLOYMENT BEREIT =====
    Datei:       valmorel.html (Single-File, alle Assets inline)
    Admin:       ?admin=true (Tabelle + PDF-Export)
    Naechste Schritte: Credentials in Zeilen 336-337 eintragen, dann pushen.
    README:      README.md (Schritt-fuer-Schritt-Anleitung)
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('README.md','utf8'); console.log(r.includes('github.io') ? 'OK: github.io-URL in README' : 'FEHLT: github.io-URL'); console.log(r.length &gt; 1000 ? 'OK: README hat Inhalt ('+r.length+' Zeichen)' : 'FEHLT: README zu kurz');"</automated>
  </verify>
  <done>README.md enthaelt github.io-URL. Terminal zeigt fertige Pages-URL oder klaren Platzhalter-Hinweis. Zusammenfassung ausgegeben.</done>
</task>

</tasks>

<verification>
- valmorel.html besteht Single-File-Pruefung (kein externer CSS/JS-Link, Base64-Assets vorhanden)
- README.md vorhanden mit Schritt-fuer-Schritt-Anleitung (JSONBin, GitHub, Pages, WhatsApp-Link)
- README nennt exakte Zeilen 336-337 fuer Credentials
- GitHub-Pages-URL steht in README (echt oder als klarer Platzhalter)
- Deployment-Zusammenfassung im Terminal ausgegeben
</verification>

<success_criteria>
- DEPL-01: valmorel.html ist vollstaendige Single-File-App (alle CSS, JS, Base64-Assets inline)
- DEPL-02: README.md vorhanden mit praeziser Deployment-Anleitung (unter 5 Minuten)
- DEPL-03: GitHub-Pages-URL ermittelt und ausgegeben (oder klarer Platzhalter mit Anweisung)
</success_criteria>

<output>
Nach Abschluss .planning/phases/02-admin-deployment/02-admin-deployment-03-SUMMARY.md erstellen.
</output>
